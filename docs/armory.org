#+title: Armory User Guide

* What Armory Is

Armory is an Org-backed local knowledge OS for AI-team work inside Emacs. A
Armory root is a directory with =index.org= plus agent, job, conversation,
task, app, import, and relationship records stored as Org files.

The main command is =M-x ogent-armory-home=. It opens a single operational UI
with metadata, health counts, navigation, recent activity, and work needing
attention. Failed work is shown in Armory Home and on the task board.

* Durable Layout

#+begin_example
armory-root/
  index.org
  imports/
  .agents/
    editor/
      persona.org
      inbox.org
      schedule.org
      jobs/
        weekly-review.org
      memory/
        context.org
        decisions.org
        learnings.org
      sessions/
        20260504T090000-weekly-review.org
#+end_example

Org is the durable source of truth. Commands edit property drawers and body
text in those Org files. Manual edits remain part of the record.

* First 10 Minutes

1. Create a Armory:

   #+begin_src emacs-lisp
   (ogent-armory-scaffold "~/armory/acme" "Acme Armory" :kind "root")
   #+end_src

2. Open the operating surface:

   #+begin_example
   M-x ogent-armory-home
   #+end_example

3. Create an agent:

   #+begin_example
   M-x ogent-armory-create-agent
   #+end_example

   Fill in name, slug, role, provider, model, workspace, tags, and persona.
   Persona files also support Armory identity fields for display name, icon,
   color, avatar, department, type, dispatch permission, adapter, effort,
   runtime mode, budget, focus paths, goals, channels, selected skills,
   recommended skills, setup status, and heartbeat state.

4. Add a recurring job:

   #+begin_example
   M-x ogent-armory-create-job
   #+end_example

   The job is written under =.agents/<slug>/jobs/<job-id>.org=.

5. Run the job:

   Open =M-x ogent-armory-jobs=, move to the job, press =R=.

6. Compose an ad hoc run:

   Use =M-x ogent-armory-compose= for a minibuffer prompt, or
   =M-x ogent-armory-compose-buffer= for an Org composer buffer. Mentions are
   resolved before the provider runs and copied into the canonical conversation
   metadata.

   #+begin_example
   @agent:cto
   @page:notes/plan.org
   @skill:review
   @job:weekly-review
   @conversation:20260506T100000-run
   #+end_example

   Attach files from the composer buffer with =C-c C-a=. Staged attachments are
   copied into =.agents/.conversations/<id>/attachments/= when the run starts.

7. Inspect the transcript:

   Open =M-x ogent-armory-conversations= and press =RET= on the new run.
   The detail buffer shows actions, metadata, runtime state, turns, prompt,
   output, tool blocks, artifacts, event logs, errors, duration, and source Org.

8. Search the Armory:

   Use =M-x ogent-armory-search=. Empty queries show an empty state. Search
   can filter by kind, agent, status, tag, and archive state from Lisp callers.

9. Browse durable data:

   Use =M-x ogent-armory-data=. The data browser lists pages, files, apps, and
   folders under the Armory root. Page commands create, rename, move, delete,
   and export Org records while preserving the source property drawer and body.

10. Open the command palette:

    Use =M-x ogent-armory-command-palette=. The palette ranks commands, pages,
    agents, jobs, conversations, apps, paths, and page text through a persisted
    derived index at =.armory-state/search.el=.

11. Open generated apps:

    Use =M-x ogent-armory-apps=. Each =index.html= below the Armory root is
    listed with label, path, modified time, and inferred owner when possible.
    HTML artifacts open through the shared Armory viewer dispatch.

12. Check git state:

    Use =M-x ogent-armory-git-status=. The status buffer lists dirty paths and
    can commit, diff, log, restore, and pull through Magit when present or plain
    git fallbacks.

13. Configure the Armory:

    Use =M-x ogent-armory-settings= for profile, provider, runtime, skills,
    storage, integration, notification, appearance, git, automation, and about
    settings. Settings are stored in =.armory-state/settings.org= and can be
    exported or imported as Org.

14. Import, demo, and back up:

    Use =M-x ogent-armory-onboard= for first-run setup, storage defaults, and
    an initial team. Use =M-x ogent-armory-registry-import= to scaffold a
    Armory from a JSON template manifest. Use =M-x ogent-armory-demo= to
    create a small example Armory. Use =M-x ogent-armory-backup= to copy
    durable Org state while skipping git metadata, process state, derived search
    cache, and previous backups.

15. Check the task board:

    Use =M-x ogent-armory-tasks=. Lanes are Inbox, Needs Reply, Running,
    Just Finished, and Archive. Scheduled and stale states appear as item
    metadata. Press =b= for board, =l= for list, and =S= for schedule view.

16. Inspect relationships:

    Use =M-x ogent-armory-status=. The graph includes armory, agent, job,
    session, app, issue-link, and Gas Town hook nodes when available. Press
    =m= for the action menu or =?= for help. Agent and job rows advertise the
    fastest edit/run actions inline.

17. Check provider adapters:

    Use =M-x ogent-armory-providers= to inspect registered runtime adapters,
    executable availability, supported modes, and resume support. Use
    =M-x ogent-armory-provider-verify= for a focused executable check.

18. Browse skills:

    Use =M-x ogent-armory-skills= to inspect the skill catalog. The catalog
    reads skill metadata from =.agents/skills/=, =skills/=, =.codex/skills/=,
    =$CODEX_HOME/skills=, and =~/.agents/skills= on demand. Refer to a skill in
    a prompt with =@skill:<key>= or pass the key through the composer.

19. Review departments and approvals:

    Use =M-x ogent-armory-org-chart= for a department tree grouped by lead and
    scope. Use =M-x ogent-armory-actions= on a conversation with pending lead
    proposals to approve, reject, or dispatch actions.

20. Open the Armory schedule:

    Use =M-x ogent-armory-schedule= for day, week, and month schedule views.
    Jobs with =OGENT_CRON=, agent heartbeats with cron heartbeats, one-shot
    jobs with =OGENT_RUN_AFTER=, and scheduled conversations all appear in one
    event stream. Past slots without a matching =OGENT_SCHEDULED_KEY= show as
    missed and can be run from the schedule buffer.

21. Bridge into Org agenda:

    Use =M-x ogent-armory-agenda= to open =org-agenda= over the Armory root,
    visible child Armories, personas, inboxes, schedules, jobs, and memory Org
    records. Job files remain normal Org =TODO= entries, and run-after tasks
    get an active =SCHEDULED= timestamp.

* Keybindings

Main ogent dispatch bindings:

| Key | Command |
|-----+---------|
| =C-c . j= / =SPC o j= | =ogent-armory-home= |
| =C-c . K= / =SPC o K= | =ogent-armory-status= |
| =C-c . y= / =SPC o y= | =ogent-armory-agents= |
| =C-c . Y= / =SPC o Y= | =ogent-armory-agent= |
| =C-c . B= / =SPC o B= | =ogent-armory-org-chart= |
| =C-c . ;= / =SPC o ;= | =ogent-armory-data= |
| =C-c . I= / =SPC o I= | =ogent-armory-tasks= |
| =C-c . O= / =SPC o O= | =ogent-armory-conversations= |
| =C-c . N= / =SPC o N= | =ogent-armory-actions= |
| =C-c . J= / =SPC o J= | =ogent-armory-schedule= |
| =C-c . Q= / =SPC o Q= | =ogent-armory-agenda= |
| =C-c . := / =SPC o := | =ogent-armory-git-status= |
| =C-c . /= / =SPC o /= | =ogent-armory-command-palette= |
| =C-c . ,= / =SPC o ,= | =ogent-armory-settings= |
| =C-c . .= / =SPC o .= | =ogent-armory-help= |
| =C-c . '= / =SPC o '= | =ogent-armory-onboard= |
| ~C-c . =~ / ~SPC o =~ | =ogent-armory-registry-import= |
| =C-c . _= / =SPC o _= | =ogent-armory-backup= |
| =C-c . V= / =SPC o V= | =ogent-armory-search= |
| =C-c . W= / =SPC o W= | =ogent-armory-apps= |
| =C-c . X= / =SPC o X= | =ogent-armory-create-agent= |
| =C-c . Z= / =SPC o Z= | =ogent-armory-create-job= |

Armory Status:

| Key | Action |
|-----+--------|
| =m= | Open the Transient action menu |
| =?= | Open status help |
| =RET= | Visit the durable Org source |
| =R= | Run or retry the selected agent, job, or session |
| =e= | Edit selected metadata in the Org property drawer |
| =E= | Visit the selected Org body or job prompt |
| =P= | Open the selected agent profile |
| =J= | Open the selected agent's jobs |
| =C= | Create a job for the selected agent |
| =g= | Refresh |
| =n= / =p= | Move between graph rows |

Armory Home:

| Key | Action |
|-----+--------|
| =RET= | Visit the item at point |
| =TAB= | Collapse or expand the section at point |
| =M-n= / =M-p= | Move between sibling sections |
| =<backtab>= | Cycle section visibility |
| =^= | Move to the parent section |
| =D= | Open the data browser |
| =h= | Open Armory git status |
| =/= | Open the command palette |
| =,= | Open Armory settings |
| =.= | Open Armory help |
| =m= | Open the Home transient menu |
| =?= | Open Home help |
| =g= | Refresh |
| =q= | Quit |

Armory Data:

| Key | Action |
|-----+--------|
| =RET= | Open the selected file through Armory viewer dispatch |
| =c= | Create an Org page |
| =r= | Rename the selected Org page |
| =m= | Move the selected Org page |
| =D= | Move the selected Org page to Armory trash |
| =e= | Export the selected Org page |
| =g= | Refresh |
| =q= | Quit |

Armory Git:

| Key | Action |
|-----+--------|
| =d= | Show diff for the selected path |
| =l= | Show log for the selected path |
| =R= | Restore the selected path |
| =c= | Commit dirty paths |
| =g= | Refresh |
| =q= | Quit |

Org chart:

| Key | Action |
|-----+--------|
| =TAB= | Collapse or expand the department at point |
| =M-n= / =M-p= | Move between departments |
| =g= | Refresh |
| =q= | Quit |

Action approvals:

| Key | Action |
|-----+--------|
| =a= | Approve action at point |
| =r= | Reject action at point |
| =A= | Approve all actions |
| =R= | Reject all actions |
| =d= | Dispatch approved actions |
| =g= | Refresh |
| =q= | Quit |

Armory Home transient menu:

| Key | Action |
|-----+--------|
| =j= | Open all jobs |
| =J= | Open jobs related to the item at point |
| =R= | Run or retry the selected agent, job, or conversation |
| =E= | Edit the selected agent persona, job prompt, or source Org record |
| =D= | Data browser |
| =a= | Agents |
| =B= | Org chart |
| =t= | Tasks |
| =c= | Conversations |
| =u= | Schedule |
| =N= | Action approvals |
| =Q= | Armory Org agenda |
| =s= | Search |
| =A= | Apps |
| =h= | Git status |
| =/= | Command palette |
| =,= | Armory settings |
| =.= | Armory help |
| =G= | Graph/status |
| ='= | Run onboarding |
| ~=~ | Import a registry template |
| =_= | Back up durable Armory state |
| =e= | Edit Armory metadata |

Armory Settings:

| Key | Action |
|-----+--------|
| =e= | Edit one setting |
| =E= | Export settings as Org |
| =I= | Import settings from Org |
| =o= | Run onboarding |
| =b= | Back up the Armory |
| =?= | Open Armory help |
| =g= | Refresh |
| =q= | Quit |

Armory Schedule:

| Key | Action |
|-----+--------|
| =RET= | Open the linked conversation or durable source Org record |
| =R= | Run the selected missed slot now |
| =d= | Day view |
| =w= | Week view |
| =m= | Month view |
| =g= | Refresh |
| =q= | Quit |
| =n= / =p= | Move between actionable rows |

In Evil normal state, =j=/=k= keep line movement, =gg=/=G= move through
the buffer, =gr= refreshes, =gj=/=gk= move between sections, and
=ZZ=/=ZQ= quit.

Agent profile:

| Key | Action |
|-----+--------|
| =RET= / =v= | Visit the file or record at point |
| =c= | Compose and run the agent manually |
| =e= | Edit identity metadata in the property drawer |
| =R= | Run the job at point or retry the conversation at point |
| =q= | Quit |

Composer buffer:

| Key | Action |
|-----+--------|
| =C-c C-c= | Submit the composed run |
| =C-c C-a= | Add an attachment |
| =C-c C-k= | Cancel |

Skill catalog:

| Key | Action |
|-----+--------|
| =RET= | Open the skill source |
| =g= | Refresh |
| =q= | Quit |

Jobs:

| Key | Action |
|-----+--------|
| =RET= | Visit source Org |
| =P= | Edit the job prompt/body |
| =e= | Edit job metadata |
| =R= | Run the job now |
| =T= | Enable or disable |
| =A= | Archive |
| =g= | Refresh |

Task board:

| Key | Action |
|-----+--------|
| =RET= | Visit source Org |
| =R= | Run or retry |
| =A= | Archive |
| =U= | Unarchive |
| =e= | Edit metadata |
| =f= | Filter |
| =g= | Refresh |

* Importing Existing Artifacts

=ogent-armory-import-artifacts= imports =.md=, =.html=, and =.txt= files from
a source folder into =imports/*.org= records. Original files are kept by
default. Markdown headings are converted to Org headings. HTML is preserved in
an Org source block.

Migration limits:

- Import is conservative and does not delete originals.
- Ambiguous metadata becomes a normal import record.
- Agent and job creation from imported metadata is intentionally narrow; use
  =ogent-armory-create-agent= and =ogent-armory-create-job= when the source
  does not identify a clear agent or job.
- Imported files become Armory Org records, but external links and rich HTML
  behavior may need manual cleanup.

* Doom Example

#+begin_src emacs-lisp
(use-package! ogent
  :defer t
  :commands (ogent-mode
             ogent-armory-home
             ogent-armory-status
             ogent-armory-agents
             ogent-armory-agent
             ogent-armory-data
             ogent-armory-tasks
             ogent-armory-conversations
             ogent-armory-actions
             ogent-armory-schedule
             ogent-armory-agenda
             ogent-armory-git-status
             ogent-armory-command-palette
             ogent-armory-settings
             ogent-armory-help
             ogent-armory-onboard
             ogent-armory-registry-import
             ogent-armory-backup
             ogent-armory-search
             ogent-armory-apps
             ogent-armory-create-agent
             ogent-armory-create-job)
  :init
  (setq ogent-enable-doom-bindings t
        ogent-doom-prefix "o")
  :config
  (ogent-setup-doom-bindings)
  (ogent-global-mode 1))
#+end_src
