#+title: Cabinet User Guide

* What Cabinet Is

Cabinet is an Org-backed local knowledge OS for AI-team work inside Emacs. A
Cabinet root is a directory with =index.org= plus agent, job, conversation,
task, app, import, and relationship records stored as Org files.

The main command is =M-x ogent-cabinet-home=. It opens a single operational UI
with metadata, health counts, navigation, recent activity, and work needing
attention. Failed work is shown in Cabinet Home and on the task board.

* Durable Layout

#+begin_example
cabinet-root/
  index.org
  imports/
  .agents/
    editor/
      persona.org
      inbox.org
      schedule.org
      jobs/
        weekly-review.org
      memory/
        context.org
        decisions.org
        learnings.org
      sessions/
        20260504T090000-weekly-review.org
#+end_example

Org is the durable source of truth. Commands edit property drawers and body
text in those Org files. Manual edits remain part of the record.

* First 10 Minutes

1. Create a Cabinet:

   #+begin_src emacs-lisp
   (ogent-cabinet-scaffold "~/cabinet/acme" "Acme Cabinet" :kind "root")
   #+end_src

2. Open the operating surface:

   #+begin_example
   M-x ogent-cabinet-home
   #+end_example

3. Create an agent:

   #+begin_example
   M-x ogent-cabinet-create-agent
   #+end_example

   Fill in name, slug, role, provider, model, workspace, tags, and persona.
   Persona files also support Cabinet identity fields for display name, icon,
   color, avatar, department, type, dispatch permission, adapter, effort,
   runtime mode, budget, focus paths, goals, channels, selected skills,
   recommended skills, setup status, and heartbeat state.

4. Add a recurring job:

   #+begin_example
   M-x ogent-cabinet-create-job
   #+end_example

   The job is written under =.agents/<slug>/jobs/<job-id>.org=.

5. Run the job:

   Open =M-x ogent-cabinet-jobs=, move to the job, press =C-c r=.

6. Compose an ad hoc run:

   Use =M-x ogent-cabinet-compose= for a minibuffer prompt, or
   =M-x ogent-cabinet-compose-buffer= for an Org composer buffer. Mentions are
   resolved before the provider runs and copied into the canonical conversation
   metadata.

   #+begin_example
   @agent:cto
   @page:notes/plan.org
   @skill:review
   @job:weekly-review
   @conversation:20260506T100000-run
   #+end_example

   Attach files from the composer buffer with =C-c C-a=. Staged attachments are
   copied into =.agents/.conversations/<id>/attachments/= when the run starts.

7. Inspect the transcript:

   Open =M-x ogent-cabinet-conversations= and press =RET= on the new run.
   The detail buffer shows actions, metadata, runtime state, turns, prompt,
   output, tool blocks, artifacts, event logs, errors, duration, and source Org.

8. Search the Cabinet:

   Use =M-x ogent-cabinet-search=. Empty queries show an empty state. Search
   can filter by kind, agent, status, tag, and archive state from Lisp callers.

9. Browse durable data:

   Use =M-x ogent-cabinet-data=. The data browser lists pages, files, apps, and
   folders under the Cabinet root. Page commands create, rename, move, delete,
   and export Org records while preserving the source property drawer and body.

10. Open the command palette:

    Use =M-x ogent-cabinet-command-palette=. The palette ranks commands, pages,
    agents, jobs, conversations, apps, paths, and page text through a persisted
    derived index at =.cabinet-state/search.el=.

11. Open generated apps:

    Use =M-x ogent-cabinet-apps=. Each =index.html= below the Cabinet root is
    listed with label, path, modified time, and inferred owner when possible.
    HTML artifacts open through the shared Cabinet viewer dispatch.

12. Check git state:

    Use =M-x ogent-cabinet-git-status=. The status buffer lists dirty paths and
    can commit, diff, log, restore, and pull through Magit when present or plain
    git fallbacks.

13. Configure the Cabinet:

    Use =M-x ogent-cabinet-settings= for profile, provider, runtime, skills,
    storage, integration, notification, appearance, git, automation, and about
    settings. Settings are stored in =.cabinet-state/settings.org= and can be
    exported or imported as Org.

14. Import, demo, and back up:

    Use =M-x ogent-cabinet-onboard= for first-run setup, storage defaults, and
    an initial team. Use =M-x ogent-cabinet-registry-import= to scaffold a
    Cabinet from a JSON template manifest. Use =M-x ogent-cabinet-demo= to
    create a small example Cabinet. Use =M-x ogent-cabinet-backup= to copy
    durable Org state while skipping git metadata, process state, derived search
    cache, and previous backups.

15. Check the task board:

    Use =M-x ogent-cabinet-tasks=. Lanes are Inbox, Needs Reply, Running,
    Just Finished, and Archive. Scheduled and stale states appear as item
    metadata. Press =c= or =C-c c= to capture a manual Inbox task, =C-c b= for
    board, =C-c l= for list, and =C-c S= for schedule view.

16. Inspect relationships:

    Use =M-x ogent-cabinet-status=. The graph includes cabinet, agent, job,
    session, app, issue-link, and Gas Town hook nodes when available. Press
    =C-c m= for the action menu or =C-c ?= for help. Agent and job rows advertise the
    fastest edit/run actions inline.

17. Check provider adapters:

    Use =M-x ogent-cabinet-providers= to inspect registered runtime adapters,
    executable availability, supported modes, and resume support. Use
    =M-x ogent-cabinet-provider-verify= for a focused executable check.

18. Browse skills:

    Use =M-x ogent-cabinet-skills= to inspect the skill catalog. The catalog
    reads skill metadata from =.agents/skills/=, =skills/=, =.codex/skills/=,
    =$CODEX_HOME/skills=, and =~/.agents/skills= on demand. Refer to a skill in
    a prompt with =@skill:<key>= or pass the key through the composer.

19. Review departments and approvals:

    Use =M-x ogent-cabinet-org-chart= for a department tree grouped by lead and
    scope. Use =M-x ogent-cabinet-actions= on a conversation with pending lead
    proposals to approve, reject, or dispatch actions.

20. Open the Cabinet schedule:

    Use =M-x ogent-cabinet-schedule= for day, week, and month schedule views.
    Jobs with =OGENT_CRON=, agent heartbeats with cron heartbeats, one-shot
    jobs with =OGENT_RUN_AFTER=, and scheduled conversations all appear in one
    event stream. Past slots without a matching =OGENT_SCHEDULED_KEY= show as
    missed and can be run from the schedule buffer.

21. Bridge into Org agenda:

    Use =M-x ogent-cabinet-agenda= to open =org-agenda= over the Cabinet root,
    visible child Cabinets, personas, inboxes, schedules, jobs, and memory Org
    records. Job files remain normal Org =TODO= entries, and run-after tasks
    get an active =SCHEDULED= timestamp.

* Keybindings

Main ogent dispatch bindings:

| Key | Command |
|-----+---------|
| =C-c . j= / =SPC o j= | =ogent-cabinet-home= |
| =C-c . K= / =SPC o K= | =ogent-cabinet-status= |
| =C-c . y= / =SPC o y= | =ogent-cabinet-agents= |
| =C-c . Y= / =SPC o Y= | =ogent-cabinet-agent= |
| =C-c . B= / =SPC o B= | =ogent-cabinet-org-chart= |
| =C-c . ;= / =SPC o ;= | =ogent-cabinet-data= |
| =C-c . I= / =SPC o I= | =ogent-cabinet-tasks= |
| =C-c . O= / =SPC o O= | =ogent-cabinet-conversations= |
| =C-c . N= / =SPC o N= | =ogent-cabinet-actions= |
| =C-c . J= / =SPC o J= | =ogent-cabinet-schedule= |
| =C-c . Q= / =SPC o Q= | =ogent-cabinet-agenda= |
| =C-c . := / =SPC o := | =ogent-cabinet-git-status= |
| =C-c . /= / =SPC o /= | =ogent-cabinet-command-palette= |
| =C-c . ,= / =SPC o ,= | =ogent-cabinet-settings= |
| =C-c . .= / =SPC o .= | =ogent-cabinet-help= |
| =C-c . '= / =SPC o '= | =ogent-cabinet-onboard= |
| ~C-c . =~ / ~SPC o =~ | =ogent-cabinet-registry-import= |
| =C-c . _= / =SPC o _= | =ogent-cabinet-backup= |
| =C-c . V= / =SPC o V= | =ogent-cabinet-search= |
| =C-c . W= / =SPC o W= | =ogent-cabinet-apps= |
| =C-c . X= / =SPC o X= | =ogent-cabinet-create-agent= |
| =C-c . Z= / =SPC o Z= | =ogent-cabinet-create-job= |

Cabinet Status:

| Key | Action |
|-----+--------|
| =C-c m= | Open the Transient action menu |
| =C-c ?= | Open status help |
| =RET= | Visit the durable Org source |
| =C-c r= | Run or retry the selected agent, job, or session |
| =C-c e= | Edit selected metadata in the Org property drawer |
| =C-c E= | Visit the selected Org body or job prompt |
| =C-c P= | Open the selected agent profile |
| =C-c J= | Open the selected agent's jobs |
| =C-c N= | Create a job for the selected agent |
| =C-c g= | Refresh |
| =C-c n= / =C-c p= | Move between graph rows |

Cabinet Home:

| Key | Action |
|-----+--------|
| =RET= | Visit the item at point |
| =TAB= | Collapse or expand the section at point |
| =M-n= / =M-p= | Move between sibling sections |
| =<backtab>= | Cycle section visibility |
| =C-c u= | Move to the parent section |
| =C-c D= | Open the data browser |
| =C-c h= | Open Cabinet git status |
| =C-c /= | Open the command palette |
| =C-c ,= | Open Cabinet settings |
| =C-c .= | Open Cabinet help |
| =C-c m= | Open the Home transient menu |
| =C-c ?= | Open Home help |
| =C-c g= | Refresh |
| =q= | Quit |

Cabinet Data:

| Key | Action |
|-----+--------|
| =RET= | Open the selected file through Cabinet viewer dispatch |
| =C-c c= | Create an Org page |
| =C-c r= | Rename the selected Org page |
| =C-c m= | Move the selected Org page |
| =C-c D= | Move the selected Org page to Cabinet trash |
| =C-c e= | Export the selected Org page |
| =C-c g= | Refresh |
| =q= | Quit |

Cabinet Git:

| Key | Action |
|-----+--------|
| =C-c d= | Show diff for the selected path |
| =C-c l= | Show log for the selected path |
| =C-c r= | Restore the selected path |
| =C-c c= | Commit dirty paths |
| =C-c g= | Refresh |
| =q= | Quit |

Org chart:

| Key | Action |
|-----+--------|
| =TAB= | Collapse or expand the department at point |
| =M-n= / =M-p= | Move between departments |
| =C-c g= | Refresh |
| =q= | Quit |

Action approvals:

| Key | Action |
|-----+--------|
| =C-c a= | Approve action at point |
| =C-c r= | Reject action at point |
| =C-c A= | Approve all actions |
| =C-c R= | Reject all actions |
| =C-c d= | Dispatch approved actions |
| =C-c g= | Refresh |
| =q= | Quit |

Cabinet Home transient menu:

| Key | Action |
|-----+--------|
| =j= | Open all jobs |
| =J= | Open jobs related to the item at point |
| =R= | Run or retry the selected agent, job, or conversation |
| =E= | Edit the selected agent persona, job prompt, or source Org record |
| =D= | Data browser |
| =a= | Agents |
| =B= | Org chart |
| =t= | Tasks |
| =c= | Conversations |
| =u= | Schedule |
| =N= | Action approvals |
| =Q= | Cabinet Org agenda |
| =s= | Search |
| =A= | Apps |
| =h= | Git status |
| =/= | Command palette |
| =,= | Cabinet settings |
| =.= | Cabinet help |
| =G= | Graph/status |
| ='= | Run onboarding |
| ~=~ | Import a registry template |
| =_= | Back up durable Cabinet state |
| =e= | Edit Cabinet metadata |

Cabinet Settings:

| Key | Action |
|-----+--------|
| =C-c e= | Edit one setting |
| =C-c E= | Export settings as Org |
| =C-c I= | Import settings from Org |
| =C-c o= | Run onboarding |
| =C-c b= | Back up the Cabinet |
| =C-c ?= | Open Cabinet help |
| =C-c g= | Refresh |
| =q= | Quit |

Cabinet Schedule:

| Key | Action |
|-----+--------|
| =RET= | Open the linked conversation or durable source Org record |
| =C-c r= | Run the selected missed slot now |
| =C-c d= | Day view |
| =C-c w= | Week view |
| =C-c m= | Month view |
| =C-c g= | Refresh |
| =q= | Quit |

In Evil normal state, bare Vim navigation/search keys keep their Evil
meanings. Cabinet display actions use the =C-c= chords above, while =RET=,
=TAB=, =M-n=/=M-p=, =q=, =ZZ=, and =ZQ= remain available where documented.

Agent profile:

| Key | Action |
|-----+--------|
| =RET= / =C-c v= | Visit the file or record at point |
| =C-c c= | Compose and run the agent manually |
| =C-c e= | Edit identity metadata in the property drawer |
| =C-c r= | Run the job at point or retry the conversation at point |
| =q= | Quit |

Composer buffer:

| Key | Action |
|-----+--------|
| =C-c C-c= | Submit the composed run |
| =C-c C-a= | Add an attachment |
| =C-c C-k= | Cancel |

Skill catalog:

| Key | Action |
|-----+--------|
| =RET= | Open the skill source |
| =C-c g= | Refresh |
| =q= | Quit |

Jobs:

| Key | Action |
|-----+--------|
| =RET= | Visit source Org |
| =C-c p= | Edit the job prompt/body |
| =C-c e= | Edit job metadata |
| =C-c r= | Run the job now |
| =C-c t= | Enable or disable |
| =C-c a= | Archive |
| =C-c g= | Refresh |

Task board:

| Key | Action |
|-----+--------|
| =c= / =C-c c= | Capture a task |
| =RET= | Visit source Org |
| =C-c r= | Run or retry |
| =C-c a= | Archive |
| =C-c u= | Unarchive |
| =C-c e= | Edit metadata |
| =C-c f= | Filter |
| =C-c g= | Refresh |

* Importing Existing Artifacts

=ogent-cabinet-import-artifacts= imports =.md=, =.html=, and =.txt= files from
a source folder into =imports/*.org= records. Original files are kept by
default. Markdown headings are converted to Org headings. HTML is preserved in
an Org source block.

Migration limits:

- Import is conservative and does not delete originals.
- Ambiguous metadata becomes a normal import record.
- Agent and job creation from imported metadata is intentionally narrow; use
  =ogent-cabinet-create-agent= and =ogent-cabinet-create-job= when the source
  does not identify a clear agent or job.
- Imported files become Cabinet Org records, but external links and rich HTML
  behavior may need manual cleanup.

* Doom Example

#+begin_src emacs-lisp
(use-package! ogent
  :defer t
  :commands (ogent-mode
             ogent-cabinet-home
             ogent-cabinet-status
             ogent-cabinet-agents
             ogent-cabinet-agent
             ogent-cabinet-data
             ogent-cabinet-tasks
             ogent-cabinet-conversations
             ogent-cabinet-actions
             ogent-cabinet-schedule
             ogent-cabinet-agenda
             ogent-cabinet-git-status
             ogent-cabinet-command-palette
             ogent-cabinet-settings
             ogent-cabinet-help
             ogent-cabinet-onboard
             ogent-cabinet-registry-import
             ogent-cabinet-backup
             ogent-cabinet-search
             ogent-cabinet-apps
             ogent-cabinet-create-agent
             ogent-cabinet-create-job)
  :init
  (setq ogent-enable-doom-bindings t
        ogent-doom-prefix "o")
  :config
  (ogent-setup-doom-bindings)
  (ogent-global-mode 1))
#+end_src
